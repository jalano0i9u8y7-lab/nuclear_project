# 系統正式上線前準備工作計劃 V8.0

## 📋 執行摘要

本文檔詳細規劃系統正式上線前的所有準備工作，確保系統能夠穩定、準確地處理 50-100 檔股票的完整分析流程。

---

## ✅ 準備工作清單（已審查與優化）

### 1. 補齊 P5 數據收集功能 ⭐ **優先級：最高**

#### 1.0 P5 Calendar 數據導入 ⭐ **新增需求**

**需求說明**：
需要將用戶提供的兩份行事曆數據導入系統：
1. **2026年全球重大財經行事曆**：CES、FOMC、NFP、四巫日、G7/G20峰會等
2. **各產業板塊風向球公司財報行事曆**：NVDA、TSLA、AAPL等龍頭公司財報日期

**數據內容**：
- **重大財經事件**：
  * 事件日期（開始/結束）
  * **歷史市場表現數據**（前5年統計）
  * **前後日期市場反映**（展前/展後、會前/會後等）
  * **建議監控的重大數據**（前置指標、關鍵時間點）
  * **追蹤建議**（技術面、籌碼面、消息面監控要點）
- **產業龍頭財報**：
  * 公司名稱、股票代碼、財報日期
  * **歷史財報表現**（前5年統計）
  * **財報前後市場反映**（財報前週、當天、後週表現）
  * **關鍵指標**（EPS、營收、毛利率等）
  * **追蹤建議**（財報前監控、當天操作、財報後策略）

**數據價值**：
- 這些歷史數據和監控建議是 P5 系統的重要參考依據
- 用於 P5 Weekly 的事件掃描和影響評估
- 用於 P5.5 Earnings Warfare 的財報風險評估
- 用於 P5 Daily 的數據收集時機判斷
- 用於事件前後的市場監控邏輯

**實施步驟**：
1. **確認 P5__CALENDAR 表格結構**
   - 檢查現有欄位是否足夠
   - 確認欄位映射關係：
     * `event_name` ← 事件名稱（如 "CES 2026", "NVDA Q4 FY2026 財報"）
     * `date_start`, `date_end` ← 事件日期（預估或確認）
     * `date_estimated` ← 日期是否為預估（true/false）⭐ **新增**
     * `date_source` ← 日期來源（"OFFICIAL", "ESTIMATED", "CALENDAR"等）⭐ **新增**
     * `event_type` ← 事件類型（"ECONOMIC_EVENT", "EARNINGS", "CONFERENCE"等）
     * `market` ← 市場（"US", "TW", "GLOBAL"等）
     * `prior_dimensions_json` ← 歷史統計、追蹤建議等詳細信息（JSON格式）
     * `status` ← 狀態（"ACTIVE"）
   - ⭐ **如果欄位不足，需要擴展表格結構**：
     * 可能需要添加 `date_estimated` 欄位（或存入 `prior_dimensions_json`）
     * 可能需要添加 `date_source` 欄位（記錄日期來源）
     * 需要確認 `last_updated` 欄位用於追蹤日期更新時間

2. **創建數據導入腳本**
   - 解析用戶提供的 Markdown 格式數據
   - 提取事件信息（日期、名稱、類型等）
   - 轉換為 P5__CALENDAR 表格格式
   - 處理 JSON 欄位（將詳細描述、歷史統計等存入 `prior_dimensions_json`）

3. **數據驗證與測試**
   - 驗證日期格式正確
   - 確認所有必要欄位都有數據
   - ⭐ **標記預估日期**：
     * 識別哪些事件的日期是預估的（如 G7 峰會、部分財報日期）
     * 標記 `date_estimated: true` 或存入 `prior_dimensions_json`
     * 記錄日期來源（`date_source: "ESTIMATED"`）
   - 測試 P5 Weekly 掃描功能是否能正確讀取這些事件

4. **整合到 P5 系統**
   - 確認 P5 Weekly 的 `P5_Calendar_ScanNextTwoWeeks` 能讀取這些事件
   - 確認 P5.5 Earnings Warfare 能讀取財報事件
   - 測試事件提醒功能
   - ⭐ **利用歷史數據**：
     * P5 Weekly 掃描事件時，讀取 `prior_dimensions_json` 中的歷史統計
     * 根據歷史市場表現計算事件權重和信心度
     * 根據前後日期市場反映，判斷事件影響時間窗口
   - ⭐ **整合監控建議**：
     * P5 Daily 在事件前後根據監控建議調整數據收集重點
     * P5 Weekly 分析時整合建議監控的重大數據
     * 實現事件前後的自動化監控邏輯
   - ⭐ **預估日期自動更新機制**：
     * 實現日期搜尋功能（從官方來源搜尋確切日期）
     * 實現日期更新功能（更新 P5__CALENDAR 表格中的日期）
     * 定期檢查預估日期是否需要更新（可以使用 P5 Daily 或 P5 Weekly 觸發）
     * 當日期更新後，通知相關模組（如 P5 Weekly、P5.5 Earnings Warfare）

**注意事項**：
- ⭐ **歷史數據的重要性**：用戶提供的歷史市場表現數據是 P5 系統預測和決策的重要依據
  * 用於計算事件權重（`prior_weight`）
  * 用於評估事件信心度（`prior_confidence`）
  * 用於生成事件影響預測
- ⭐ **前後日期市場反映**：這些數據用於：
  * P5 Weekly 掃描時判斷事件影響時間窗口
  * P5 Daily 在事件前後調整數據收集重點
  * P5.5 Earnings Warfare 判斷財報前後風險時機
- ⭐ **建議監控的重大數據**：這些信息用於：
  * P5 Daily 數據收集時機判斷（何時收集哪些數據）
  * P5 Weekly 分析時整合相關數據
  * 事件前後的自動化監控邏輯
- 用戶提供的數據非常詳細（包含歷史統計、追蹤建議等），這些信息應存入 `prior_dimensions_json`
- 重大財經事件和財報事件可能需要不同的 `event_type` 分類
- 需要考慮事件的重複性（如 FOMC 每季都有，財報每季都有）
- ⭐ **預估日期自動更新機制**：
  * 大多數沒有公布確切日期的事件目前都是預估
  * 系統需要能夠搜尋並更新正確日期（當官方公布後）
  * 需要標記哪些事件是「預估日期」（`date_estimated: true`）
  * 需要實現日期更新功能（`updateEventDate(eventId, confirmedDate)`）
  * 需要定期檢查並更新預估日期（可以使用 P5 Daily 或 P5 Weekly 觸發）

**建議的 event_type 分類**：
- `ECONOMIC_EVENT`：重大財經事件（FOMC、NFP、CPI等）
- `EARNINGS`：財報（NVDA、TSLA等公司財報）
- `CONFERENCE`：展會（CES、MWC等）
- `POLITICAL_EVENT`：政治事件（G7、G20峰會等）
- `HOLIDAY`：假期（感恩節、聖誕節等）

#### 1.1 當前狀態檢查
- ✅ **Stooq Cloud 代理**：已實現（`24_P5_DAILY_OHLCV_STOOQ.js`）
  - 使用 `CLOUD_FUNCTION_STOOQ_URL` 配置
  - 需要確認 Cloud Function 已部署並可訪問
- ✅ **CSE 配置**：已設定（`02_M0_CONFIG.js`）
  - `P5_OHLCV`、`P5_SECTOR_ETF`、`P5_DERIVATIVES`、`P5_NEWS`、`P5_MARKET`
  - 需要確認所有 CSE CX ID 已配置到 `PropertiesService`

#### 1.2 待完成項目
- [ ] **驗證 Cloud Function 代理可用性**
  - 測試 stooq.com 數據收集是否正常
  - 確認錯誤處理機制（fallback 到 CSE）
- [ ] **補齊財務數據收集**（P2 所需）
  - 實現 `collectTaiwanStockFinancialData`（使用 P2_TAIWAN CSE）
  - 實現 `collectUSStockFinancialData`（使用 SEC EDGAR）
  - 實現 `collectJapanStockFinancialData`（使用 P2_JAPAN CSE）
  - ⚠️ **注意**：目前這些函數調用 `executeCSESearch`，但該函數可能未實現
- [ ] **測試五檔股票的完整數據收集**
  - NVDA（美股）
  - TSM（美股）
  - 000660.KS（韓股）
  - AMD（美股）
  - 3711.TW（台股）

#### 1.3 實施步驟
1. **檢查並配置所有 CSE CX ID**
   ```javascript
   // 需要在 PropertiesService 中配置：
   // - GOOGLE_CSE_CX_P5_OHLCV
   // - GOOGLE_CSE_CX_P5_SECTOR_ETF
   // - GOOGLE_CSE_CX_P5_DERIVATIVES
   // - GOOGLE_CSE_CX_P5_NEWS
   // - GOOGLE_CSE_CX_P5_MARKET
   // - GOOGLE_CSE_CX_P2_TAIWAN
   // - GOOGLE_CSE_CX_P2_JAPAN
   // - GOOGLE_CSE_CX_INSTITUTIONAL_DATA
   ```

2. **驗證 Cloud Function 代理**
   - 確認 `CLOUD_FUNCTION_STOOQ_URL` 已配置
   - 測試單檔股票數據收集（如 NVDA）
   - 確認錯誤處理和 fallback 機制

3. **實現財務數據收集函數**
   - 檢查 `executeCSESearch` 是否已實現（在 `03_M0_CORE.js` 中）
   - 如果未實現，需要實現或改用其他方式（如直接調用 M0 Job Queue）
   - 測試財務數據收集是否成功

4. **完整測試五檔股票**
   - 執行 `P5_Daily_Execute` 收集所有數據
   - 確認 `MARKET_OHLCV_DAILY`、`MARKET_INDICATORS_DAILY` 等表格有數據
   - 確認財務數據收集成功（用於 P2）

---

### 2. 跑完完整範例五檔，確認所有邏輯無誤 ⭐ **優先級：最高**

#### 2.1 測試範圍
- [ ] **P0 → P0.7 → P1 → P2 → P2.5 → P3 → P4** 完整流程
- [ ] **P5 Daily** 數據收集
- [ ] **P5 Weekly** 策略生成
- [ ] **數據寫入驗證**：確認所有表格正確寫入

#### 2.2 測試檢查點

**Phase 0 系列**
- [ ] P0 輸出：`themes`、`subthemes`、`key_nodes`、`industry_chain`、`capital_flow`
- [ ] P0.5 輸出：產業鏈地圖、供應鏈情報（如果已實現）
- [ ] P0.7 輸出：`narrative_states`、`loop_dominances`、`time_positions`、`leveraged_roles`

**Phase 1**
- [ ] `Phase1_Master_Candidates` 表格有 5 檔股票
- [ ] 每檔股票都有完整的 `Theme_Track`、`Moat_Type`、`Rerate_State` 等欄位

**Phase 2**
- [ ] `Phase2_Output` 表格有 5 檔股票的 Gate 檢查結果
- [ ] 財務指標欄位有數據（或明確標記為缺失）
- [ ] `Tier` 分層符合邏輯（Gate=PASS 才能進入 CORE/STABLE_SWING/AGGRESSIVE）
- [ ] `Tier_Reason` 說明清晰

**Phase 2.5**（可選）
- [ ] `Phase2.5_Output` 表格有 Smart_Money_Score
- [ ] 機構級數據收集成功

**Phase 3**
- [ ] `Phase3_Output` 表格有技術分析結果
- [ ] Cat 分類正確
- [ ] Buy 價格計算正確

**Phase 4**
- [ ] `P4__SNAPSHOT` 有資金配置結果
- [ ] 配置百分比符合分層規則

**P5 Daily**
- [ ] `MARKET_OHLCV_DAILY` 有最新數據
- [ ] `MARKET_INDICATORS_DAILY` 有技術指標
- [ ] `P5__DAILY_STATUS` 有執行狀態

**P5 Weekly**
- [ ] `P5__WEEKLY_SNAPSHOT` 有策略快照
- [ ] `P5__WEEKLY_STOCK_STRATEGIES` 有個股策略

#### 2.3 實施步驟
1. **執行完整流程**
   - 使用 UI 的「執行完整流程」按鈕
   - 或使用 `UI_ExecuteFullPipeline()`
   - 記錄執行時間和錯誤

2. **逐項檢查輸出**
   - 檢查每個 Phase 的輸出表格
   - 確認數據完整性
   - 驗證邏輯正確性

3. **修正發現的問題**
   - 記錄所有問題
   - 逐一修正
   - 重新測試直到無誤

---

### 3. 測試 UI 功能 ⭐ **優先級：高**

#### 3.0 Phase 結果審查功能 ⭐ **新增需求**

**需求說明**：
每個 Phase（P0-P4）完成後，需要：
1. 展現結果給使用者看
2. 讓使用者審查是否 OK
3. 讓使用者提出意見
4. 確認 OK 才往下走
5. 展現結果的同時可以詢問使用者選項或意見

**各 Phase 審查需求**：

**P0 審查**：
- [ ] 顯示分析出的產業面向列表
- [ ] 詢問使用者：決定分析出幾個產業面向（可以調整）
- [ ] 詢問使用者：產業面向是否有偏好（可以選擇特定產業）
- [ ] 顯示 P0 分析結果摘要
- [ ] 詢問使用者：結果是否 OK（可以選擇繼續、修改、重新執行）

**P1 審查**：
- [ ] 顯示每個產業面的候選公司列表
- [ ] 詢問使用者：每個產業面的公司選擇標準
- [ ] 詢問使用者：美/日/台公司的比例是多少
- [ ] 詢問使用者：每個產業面最後需要篩選出幾間（可以調整）
- [ ] 顯示 P1 篩選結果（Master_Candidates, Tracking_Pool, Rejection_Pool）
- [ ] 詢問使用者：結果是否 OK（可以選擇繼續、修改、重新執行）

**P2 審查**：
- [ ] 顯示 Gate 檢查結果
- [ ] 顯示 Tier 分層結果
- [ ] 詢問使用者：分層是否合理（可以手動調整）
- [ ] 詢問使用者：結果是否 OK

**P3 審查**：
- [ ] 顯示技術分析結果
- [ ] 顯示 Cat 分類結果
- [ ] 顯示 Buy 價格建議
- [ ] 詢問使用者：結果是否 OK

**P4 審查**：
- [ ] 顯示資金配置結果
- [ ] 顯示配置百分比
- [ ] 詢問使用者：配置是否合理（可以手動調整）
- [ ] 詢問使用者：結果是否 OK

**實施步驟**：
1. **設計 Phase Review 資料結構**
   - 創建 `PHASE_REVIEW` 表格（類似 `PRE_EXECUTION_CONFIRMATION`）
   - 欄位：`review_id`, `phase`, `snapshot_id`, `result_json`, `questions_json`, `answers_json`, `status`, `created_at`, `updated_at`

2. **實現 Phase Review 核心函數**
   - `savePhaseReview(phase, snapshotId, result, questions)` - 保存 Phase 結果和問題
   - `getPhaseReview(reviewId)` - 獲取審查記錄
   - `submitPhaseReview(reviewId, answers)` - 提交使用者答案
   - `generatePhaseReviewQuestions(phase, result)` - 根據 Phase 類型生成問題

3. **修改 UI_ExecuteFullPipeline**
   - 每個 Phase 完成後，調用 `savePhaseReview`
   - 暫停流程，等待使用者審查
   - 讀取使用者答案，決定是否繼續或修改

4. **創建 UI 審查頁面**
   - 在 `25_UI_SIDEBAR.html` 中添加「Phase 審查」頁面
   - 顯示 Phase 結果（格式化顯示）
   - 顯示問題表單（根據 Phase 類型動態生成）
   - 提供「確認繼續」、「修改重跑」、「跳過此 Phase」按鈕

5. **整合到執行流程**
   - 修改 `UI_ExecuteFullPipeline` 以支持階段性審查
   - 每個 Phase 完成後檢查是否需要審查
   - 如果 `skip_review: false`，暫停並顯示審查頁面
   - 等待使用者確認後繼續下一個 Phase

**注意事項**：
- 需要考慮非同步執行的問題（Phase 可能在背景執行）
- 需要實現輪詢機制檢查審查狀態
- 需要支持「跳過審查」選項（`skip_review: true`）
- 需要保存審查歷史（用於追蹤和學習）

#### 3.1 本周策略生成測試
- [ ] **P5 Weekly 執行**
  - 確認 `P5__WEEKLY_STOCK_STRATEGIES` 表格有數據
  - 確認 UI 能正確顯示策略
- [ ] **策略顯示**
  - 檢查 UI 側邊欄的「本周策略」頁面
  - 確認策略內容格式正確

#### 3.2 Human Signal 輸入測試
- [ ] **UI 輸入功能**
  - 測試 `25_UI_SIDEBAR.html` 的 Human Signal 表單
  - 確認提交成功
- [ ] **數據保存**
  - 確認 `HUMAN_SIGNAL` 表格有新增記錄
  - 確認所有欄位正確保存
- [ ] **融入資訊來源因子**
  - 確認 P5 Daily 或 P5 Weekly 能讀取 `HUMAN_SIGNAL` 表格
  - 確認 Human Signal 能影響分析結果
  - ⚠️ **注意**：需要檢查 P5 Daily/Weekly 是否有讀取 `HUMAN_SIGNAL` 的邏輯

#### 3.3 緊急通知測試
- [ ] **P5.4 警報檢測**
  - 確認 `24_P5_DAILY_ALERT.js` 已實現
  - 確認 P5 Daily 能觸發警報
  - 確認 `P5__DAILY_STATUS` 的 `alerts_json` 欄位有數據
- [ ] **通知機制**
  - 實現緊急通知測試檔（`TEST_EMERGENCY_ALERT.js`）
  - 測試 Email 通知（使用 `MailApp.sendEmail`）
  - 考慮手機通知方案：
    - **方案 1**：Email → 手機 Email 推送（最簡單）
    - **方案 2**：使用 Google Apps Script 的 `UrlFetchApp` 調用第三方 API（如 Pushover、Telegram Bot）
    - **方案 3**：使用 Google Cloud Functions 調用 Firebase Cloud Messaging（FCM）
- [ ] **UI 顯示**
  - 確認 UI 側邊欄的「緊急通知」頁面能顯示警報
  - 確認通知內容格式正確

#### 3.4 實施步驟
1. **創建緊急通知測試檔**
   ```javascript
   // TEST_EMERGENCY_ALERT.js
   function testEmergencyAlert() {
     // 模擬觸發緊急警報
     // 測試通知發送
   }
   ```

2. **實現通知機制**
   - 選擇通知方案（建議先使用 Email）
   - 實現通知發送函數
   - 整合到 P5 Daily Alert 模組

3. **測試完整流程**
   - 觸發測試警報
   - 確認通知發送成功
   - 確認 UI 顯示正確

---

### 4. AI Prompt 精準度對齊與 Batch 設定 ⭐ **優先級：高**

#### 4.0 ⭐⭐⭐⭐⭐ **P3 機構級預測視角原則（最高優先級）**

**核心原則**：P3 技術分析模組的本質不是「技術分析」，而是「機構級預測」

**Prompt 對齊檢查清單**（必須嚴格遵循）：
- [ ] **視角檢查**：Prompt 是否以機構主力視角設計？（高盛、摩根、大型對沖基金等）
- [ ] **目標檢查**：Prompt 是否強調「預測未來」而非「技術分析」？
- [ ] **邏輯檢查**：Prompt 是否強調「量大於價」和「解釋主力行為」？
- [ ] **禁止檢查**：Prompt 是否明確禁止輸出「根據 RSI、MACD、均線、支撐壓力、黃金分割」等程式就能算的結論？
- [ ] **數據檢查**：Prompt 是否說明技術指標已由程式計算好（從 MARKET_INDICATORS_DAILY 讀取），直接使用，不要重新計算？
- [ ] **數據檢查**：Prompt 是否整合使用 P2.5 的機構級數據？（Smart_Money_Score、機構持倉、Dark Pool、Options Flow）
- [ ] **輸出檢查**：Prompt 是否要求輸出「主力行為解釋」、「意圖判斷」、「未來預測」？
- [ ] **AI 必要性**：Prompt 是否強調不能用程式公式替代，需要 AI 模型進行複雜推理？
- [ ] **禁止解釋過去**：Prompt 是否明確禁止把資源拿去解釋過去（例如：解釋為什麼過去漲跌）？

**關鍵要求**：
- ✅ **不是「分析主力畫出來的線」，而是「把自己當成是主力去解釋和預測」**
- ✅ **要「成為主力的大腦」，解釋為什麼主力會做這些操作**
- ✅ **預測主力的真正意圖以及未來會做的操作與市場變化**
- ✅ **跟著主力來順風獲利，而非只是單純程式就能做的技術分析**

**詳細原則文檔**：請參考 `系統核心設計原則_V8.0.md`

---

#### 4.1 Prompt 精準度對齊 ⭐ **最高優先級**

**核心原則**：請參考 `系統核心設計原則_V8.0.md`

- [ ] **檢查所有 Phase 的 Prompt**
  - P0: `09_P0_INDUSTRY_ENGINEERING.js` 的 `buildP0Prompt`
  - P0.7: `19_P0_7_SYSTEM_DYNAMICS.js` 的 Prompt
  - P1: `20_P1_COMPANY_POOL.js` 的 Prompt
  - P2: `21_P2_FUNDAMENTAL_ANALYSIS.js` 的 Prompt
  - P2.5: `21_P2_5_PROMPT.js` 的 Prompt
  - P3: `22_P3_AI_ANALYSIS.js` 的 Prompt ⭐ **最高優先級**
  - P5 Weekly: `24_P5_WEEKLY_PROMPT.js` 的 Prompt
- [ ] **對齊 V8.0 架構要求**
  - 確認 Prompt 符合「基本面是底，籌碼面是因，技術面是果」的哲學
  - 確認 Prompt 明確要求不包含籌碼面分析（P0、P0.7、P1、P2）
  - 確認 Prompt 明確要求包含籌碼面分析（P2.5、P3）
- [ ] **P2 同業比較與相對位置原則** ⭐⭐⭐⭐⭐ **最高優先級**
  - [ ] **同業比較檢查**：Prompt 是否強調「重點不是絕對數值，而是相對位置」？
  - [ ] **同業定義檢查**：Prompt 是否說明同業不是「同板塊」，而是「相同細分產業」？
  - [ ] **同業分類檢查**：Prompt 是否要求分開龍頭/中大型/小型新創來比較，禁止跨分類比較？
  - [ ] **兩階段流程檢查**：Prompt 是否明確說明兩階段流程（AI 識別同業 → 程式計算相對位置）？
  - [ ] **Stage 1 任務檢查**：Prompt 是否要求 AI 只執行 Stage 1（識別同業清單），不需要計算相對位置？
  - [ ] **同業指標提取檢查**：Prompt 是否要求同時提取目標公司和同業公司的財務指標？
  - [ ] **Gate 檢查**：Prompt 是否說明 Gate 檢查基於同業比較結果，而非絕對數值？
  - [ ] **數據來源檢查**：Prompt 是否要求同業數據從相同白名單數據源抓取？
  - [ ] **輸出檢查**：Prompt 是否要求輸出同業公司清單（包含規模分類和選擇理由）？
  - [ ] **程式計算檢查**：是否實現了 Stage 2 的程式計算函數（`calculateRelativePositions`、`judgeStructuralAdvantage`、`judgeHeterogeneityRisk`）？
- [ ] **P3 機構級預測視角原則** ⭐⭐⭐⭐⭐ **最高優先級**
  - [ ] **視角檢查**：Prompt 是否以機構主力視角設計？（高盛、摩根、大型對沖基金等）
  - [ ] **目標檢查**：Prompt 是否強調「預測未來」而非「技術分析」？
  - [ ] **邏輯檢查**：Prompt 是否強調「量大於價」和「解釋主力行為」？
  - [ ] **禁止檢查**：Prompt 是否明確禁止輸出「根據 RSI、MACD、均線、支撐壓力」等程式就能算的結論？
  - [ ] **數據檢查**：Prompt 是否說明技術指標已由程式計算好，直接使用？
  - [ ] **數據檢查**：Prompt 是否整合使用 P2.5 的機構級數據？
  - [ ] **輸出檢查**：Prompt 是否要求輸出「主力行為解釋」、「意圖判斷」、「未來預測」？
- [ ] **白名單數據源原則** ⭐⭐⭐⭐⭐ **最高優先級**
  - [ ] **P2 Prompt**：是否明確說明「所有數據類的資料一律不由 AI 模型自己搜尋，全部由程式從白名單數據源獲取」？
  - [ ] **P2 Prompt**：是否說明財務數據已由程式從白名單 CSE 收集？
  - [ ] **P3 Prompt**：是否明確說明「所有數據類的資料一律不由 AI 模型自己搜尋，全部由程式從白名單數據源獲取」？
  - [ ] **P3 Prompt**：是否說明歷史 OHLCV 數據已由程式從白名單數據源（stooq.com、TWSE、TPEX）收集？
  - [ ] **P3 Prompt**：是否說明 P2 財務指標已繼承，不需要重複搜尋與計算？
  - [ ] **P3 Prompt**：是否說明技術指標已由程式計算，不要重新計算？
- [ ] **P3 讀取歷史 OHLCV 數據** ⭐⭐⭐⭐⭐ **最高優先級**
  - [ ] **數據收集函數**：`collectTechnicalDataFromExternalSources` 是否調用 `getHistoricalOHLCV`？
  - [ ] **優先讀取表格**：是否優先從 `MARKET_OHLCV_DAILY` 表格讀取（已持倉個股可以使用留存的資料）？
  - [ ] **自動補充**：如果數據不足，是否自動從 stooq.com 補充（通過 Cloud Function 代理）？
  - [ ] **數據傳遞**：是否將歷史 OHLCV 數據傳遞給 AI 模型？

#### 4.2 Batch 設定（關鍵：處理 50-100 檔股票）

**GAS 環境限制**
- ⏱️ **執行時間限制**：6 分鐘（免費版）或 30 分鐘（付費版）
- 📊 **記憶體限制**：約 1GB
- 🔄 **並發限制**：同時只能執行 1 個腳本（免費版）或 30 個（付費版）
- 📝 **寫入限制**：每秒最多 10 次寫入操作

**AI 模型限制**
- **OpenAI**：Rate Limit 取決於 Tier
- **Claude**：Rate Limit 取決於計劃
- **Gemini**：Rate Limit 取決於計劃

**Batch 策略**
- [ ] **P2 批量處理**
  - 目前：逐檔處理（可能超時）
  - 建議：每批 5-10 檔，批次間延遲 1-2 秒
  - 使用 `Utilities.sleep()` 控制節奏
- [ ] **P3 批量處理**
  - 目前：逐檔處理
  - 建議：每批 5-10 檔
- [ ] **P5 Weekly 個股策略**
  - 已實現 Batch 機制（`24_P5_WEEKLY_STOCK_STRATEGY.js`）
  - 確認 Batch 大小合適（建議 5-10 檔/批）
- [ ] **P5 Daily 數據收集**
  - 已實現 Batch 機制（每批 10 檔）
  - 確認延遲時間合適（目前 0.5 秒）

**實施步驟**
1. **檢查現有 Batch 實現**
   - 確認所有批量處理都有適當的批次大小
   - 確認批次間有適當的延遲

2. **測試 50-100 檔股票**
   - 創建測試腳本，模擬 50-100 檔股票
   - 執行完整流程，監控執行時間
   - 確認不超過 GAS 執行時間限制

3. **優化 Batch 策略**
   - 根據測試結果調整批次大小
   - 根據測試結果調整延遲時間
   - 考慮使用 Trigger 分散執行（如果超過時間限制）

---

### 5. 成本估算 ⭐ **優先級：中**

#### 5.1 執行頻率假設
- **P0/P0.7/P1**：每季度 1 次（8-10 個產業面）
- **P2**：每月 1 次（50-100 檔股票）
- **P2.5**：每月 1 次（50-100 檔股票，可選）
- **P3**：每週 1 次（50-100 檔股票）
- **P4**：每週 1 次（純程式計算，無 AI 成本）
- **P5 Daily**：每日 1 次（50-100 檔股票）
- **P5 Weekly**：每週 1 次（50-100 檔股票）
- **P5 Monthly**：每月 1 次
- **P5 Quarterly**：每季度 1 次（觸發 P0-P4 重跑）

#### 5.2 成本估算（基於現有配置）

**AI 模型成本**（每 1K tokens）
- GPT-5.2: $0.030
- Claude Sonnet 4.5: $0.015
- Claude Opus 4.5: $0.075
- O3: $0.060
- Gemini 3.0 Pro: $0.002

**月度成本估算**（假設 50-100 檔股票）

| Phase | 執行頻率 | 每次 Token 估算 | 每次成本 | 月度成本 |
|-------|---------|----------------|---------|---------|
| P0 | 1/3 月 | 50K (Opus) + 20K (GPT) | $4.50 | $1.50 |
| P0.7 | 1/3 月 | 30K (O3) + 20K (Opus) | $2.70 | $0.90 |
| P1 | 1/3 月 | 80K (Sonnet) + 30K (GPT) | $2.10 | $0.70 |
| P2 | 1/月 | 40K (Sonnet) × 100檔 + 20K (GPT) | $62.00 | $62.00 |
| P2.5 | 1/月 | 30K (Sonnet) × 100檔 + 15K (GPT) | $46.50 | $46.50 |
| P3 | 4/月 | 20K (Sonnet) × 100檔 + 10K (GPT) | $31.00 | $124.00 |
| P5 Daily | 30/月 | 10K (GPT) × 100檔 + 5K (Gemini) | $3.05 | $91.50 |
| P5 Weekly | 4/月 | 50K (Sonnet) × 100檔 + 20K (GPT) | $77.00 | $308.00 |
| P5 Monthly | 1/月 | 30K (Sonnet) + 15K (GPT) | $0.75 | $0.75 |
| **總計** | | | | **$635.85/月** |

⚠️ **注意**：以上估算基於假設，實際成本可能因以下因素而異：
- Token 使用量可能因股票數量、市場複雜度而變化
- 某些 Phase 可能不需要處理所有股票（如 P2.5 可選）
- 實際執行頻率可能因市場情況而調整

#### 5.3 數據訂閱成本

**初期（免費規劃）**
- Google CSE：免費（每日 100 次搜尋）
- Stooq.com：免費（通過 Cloud Function 代理）
- SEC EDGAR：免費
- 其他官方數據源：免費

**中期（訂閱規劃）**
- **Bloomberg Terminal**：$2,000-2,500/月（機構級數據）
- **Refinitiv Eikon**：$1,500-2,000/月（機構級數據）
- **FactSet**：$1,200-1,800/月（機構級數據）
- **Google CSE 付費版**：$5/月（每日 10,000 次搜尋）

**建議**
- 初期使用免費數據源，確認系統穩定運行
- 中期根據實際需求考慮訂閱機構級數據源
- 優先考慮 Google CSE 付費版（成本低，提升大）

#### 5.4 成本優化建議
1. **減少不必要的 AI 調用**
   - P2.5 可選，只在需要時執行
   - P3 可以只處理有變動的股票
   - P5 Daily 可以只處理持倉股票

2. **優化 Prompt 長度**
   - 減少冗餘內容
   - 使用更精簡的 Prompt

3. **批量處理優化**
   - 減少批次間的延遲時間（在符合 Rate Limit 的前提下）
   - 使用更高效的數據結構

---

### 6. 正式上線測試 ⭐ **優先級：最高**

#### 6.1 測試規模
- **產業面**：8-10 個
- **每產業股票數**：5-10 檔（大產業 15-20 檔）
- **總股票數**：50-100 檔

#### 6.2 測試檢查點
- [ ] **執行時間**：確認不超過 GAS 限制
- [ ] **數據完整性**：確認所有表格正確寫入
- [ ] **邏輯正確性**：確認所有 Phase 的邏輯符合預期
- [ ] **錯誤處理**：確認錯誤不會導致系統崩潰
- [ ] **成本監控**：確認成本在預算範圍內

#### 6.3 實施步驟
1. **準備測試環境**
   - 創建測試 Google Sheet
   - 配置所有必要的 API Keys 和 CSE CX IDs
   - 準備測試數據（8-10 個產業面，50-100 檔股票）

2. **執行完整測試**
   - 執行 P0 → P4 完整流程
   - 執行 P5 Daily/Weekly/Monthly
   - 監控執行時間和成本

3. **驗證結果**
   - 檢查所有輸出表格
   - 驗證邏輯正確性
   - 確認成本在預算範圍內

4. **修正問題**
   - 記錄所有問題
   - 逐一修正
   - 重新測試直到無誤

---

## 🔧 技術實施細節

### 7.0 P5 Calendar 數據導入實現

**問題**：用戶提供了兩份詳細的行事曆數據（Markdown 格式），需要導入到 P5__CALENDAR 表格。

**解決方案**：

1. **創建數據導入腳本**
   ```javascript
   // IMPORT_P5_CALENDAR_2026.js
   function importP5Calendar2026() {
     // 解析用戶提供的 Markdown 數據
     // 轉換為 P5__CALENDAR 表格格式
     // 批量導入到 Google Sheets
   }
   ```

2. **數據結構映射**
   - **重大財經事件**：
     * `event_type` = "ECONOMIC_EVENT"
     * `event_name` = "CES 2026" / "FOMC 2026-01-28" / "NFP 2026-01-09" 等
     * `date_start`, `date_end` = 事件日期
     * `market` = "US" / "GLOBAL" 等
     * `pre_window` = 事件前監控窗口（天數，如 7、14、30）
     * `post_window` = 事件後監控窗口（天數，如 7、14）
     * `prior_weight` = 根據歷史統計計算的權重（0-1）
     * `prior_confidence` = 根據歷史統計計算的信心度（0-1）
     * `prior_dimensions_json` = {
       * `description`: 事件描述
       * `historical_stats`: {
         * `pre_period_performance`: 事件前市場表現（過去5年統計）
         * `event_day_performance`: 事件當天市場表現
         * `post_period_performance`: 事件後市場表現
         * `success_rate`: 上漲/下跌機率
         * `average_impact`: 平均影響幅度
         * `volatility_impact`: 波動率影響
       * }
       * `monitoring_suggestions`: {
         * `pre_event_data`: 事件前建議監控的數據（如 CPI、NFP、期權活動等）
         * `event_day_focus`: 事件當天重點關注（如 FOMC 聲明稿關鍵字、點陣圖等）
         * `post_event_data`: 事件後建議監控的數據
         * `key_indicators`: 關鍵指標列表
       * }
       * `tracking_suggestions`: 追蹤建議（技術面、籌碼面、消息面）
       * `market_impact_analysis`: 市場影響分析
     * }
   
   - **產業龍頭財報**：
     * `event_type` = "EARNINGS"
     * `event_name` = "NVDA Q4 FY2026 財報" / "TSLA Q4 2025 財報" 等
     * `date_start` = 財報日期（通常只有開始日期）
     * `market` = "US" / "TW" 等
     * `pre_window` = 財報前監控窗口（通常 7-14 天）
     * `post_window` = 財報後監控窗口（通常 7-14 天）
     * `prior_weight` = 根據歷史財報表現計算的權重
     * `prior_confidence` = 根據歷史財報表現計算的信心度
     * `prior_dimensions_json` = {
       * `company`: 公司名稱
       * `ticker`: 股票代碼
       * `quarter`: 季度
       * `historical_performance`: {
         * `pre_earnings_performance`: 財報前一週/兩週表現（過去5年統計）
         * `earnings_day_performance`: 財報當天表現（平均波動、上漲/下跌機率）
         * `post_earnings_performance`: 財報後一週/兩週表現
         * `beat_rate`: 超預期機率
         * `average_volatility`: 平均波動幅度
       * }
       * `key_metrics`: {
         * `primary_metrics`: 主要指標（EPS、營收、毛利率等）
         * `secondary_metrics`: 次要指標（用戶數、訂閱數等）
         * `guidance_focus`: 指引重點
       * }
       * `monitoring_suggestions`: {
         * `pre_earnings_data`: 財報前建議監控（如交車數據、供應鏈動態、分析師預期調整）
         * `earnings_day_focus`: 財報當天重點（如 CEO 發言基調、指引、Q&A）
         * `post_earnings_data`: 財報後建議監控（如分析師報告、目標價調整）
       * }
       * `tracking_suggestions`: 追蹤建議（技術面、籌碼面、消息面）
     * }

3. **實現步驟**
   - 創建 `IMPORT_P5_CALENDAR_2026.js` 腳本
   - 手動解析 Markdown 數據（或使用簡單的正則表達式）
   - 批量插入到 P5__CALENDAR 表格
   - 驗證數據完整性

4. **注意事項**
   - ⭐ **歷史數據的利用**：
     * 根據歷史市場表現計算 `prior_weight` 和 `prior_confidence`
     * 將歷史統計數據完整存入 `prior_dimensions_json`，供 P5 系統參考
     * 前後日期市場反映數據用於判斷事件影響時間窗口
   - ⭐ **監控建議的整合**：
     * 將「建議監控的重大數據」存入 `monitoring_suggestions`
     * P5 Daily 在事件前後根據這些建議調整數據收集重點
     * P5 Weekly 分析時整合這些監控數據
   - 用戶提供的數據非常詳細，建議將詳細信息存入 `prior_dimensions_json`
   - 需要處理日期格式轉換
   - 需要生成唯一的 `event_id`
   - 需要計算 `pre_window` 和 `post_window`（根據歷史數據中的前後日期反映）
   - 建議先導入部分數據測試，確認無誤後再批量導入

5. **後續整合到 P5 功能**
   - **P5 Weekly 事件掃描**：
     * 讀取 `prior_dimensions_json` 中的歷史統計
     * 根據歷史表現評估事件影響
     * 根據前後日期市場反映，判斷事件影響時間窗口
   - **P5.5 Earnings Warfare**：
     * 讀取財報事件的歷史表現
     * 根據歷史波動率評估財報風險
     * 根據監控建議，在財報前後調整監控重點
   - **P5 Daily 數據收集**：
     * 根據事件前後的監控建議，調整數據收集重點
     * 在事件前後收集建議監控的重大數據
   - **事件權重計算**：
     * 根據歷史統計數據自動計算 `prior_weight` 和 `prior_confidence`
     * 用於 P5 Weekly 的事件影響評估
   - ⭐ **預估日期自動更新**：
     * **日期搜尋功能**：
       - 從官方來源搜尋確切日期（SEC、EDINET、公司 IR 官網等）
       - 使用 CSE 搜尋功能（`EARNINGS_CALENDAR` CSE）
       - 對於重大財經事件，搜尋官方公告（FOMC、ECB、BOJ 等）
     * **日期更新功能**：
       - `updateEventDate(eventId, confirmedDate, dateSource)` - 更新事件日期
       - 更新 `date_start`, `date_end` 欄位
       - 更新 `date_estimated: false`, `date_source: "OFFICIAL"`
       - 更新 `last_updated` 時間戳
     * **定期檢查機制**：
       - 可以使用 P5 Daily 或 P5 Weekly 觸發日期檢查
       - 掃描所有 `date_estimated: true` 的事件
       - 對於即將到來的事件（30天內），優先檢查並更新
       - 記錄更新日誌（用於追蹤）
     * **更新後處理**：
       - 當日期更新後，通知相關模組（如 P5 Weekly、P5.5 Earnings Warfare）
       - 如果日期變更較大（>7天），可能需要重新計算事件影響窗口

### 7.1 財務數據收集實現

**問題**：目前 `collectFinancialDataFromExternalSources` 調用 `executeCSESearch`，但該函數可能未實現。

**解決方案**：
1. **檢查 `executeCSESearch` 是否已實現**
   - 在 `03_M0_CORE.js` 中搜尋
   - 如果未實現，需要實現或改用其他方式

2. **替代方案**：通過 M0 Job Queue 提交 CSE_SEARCH 任務
   ```javascript
   // 在 collectFinancialDataFromExternalSources 中
   const jobId = `P2_FINANCIAL_${ticker}_${Date.now()}`;
   const m0Payload = {
     search_query: searchQuery,
     cse_type: cseType,
     max_results: 10,
     ticker: ticker,
     market: market,
     frequency: frequency
   };
   
   // 提交到 M0 Job Queue
   submitToM0JobQueue("CSE_SEARCH", ["GEMINI_SEARCH"], m0Payload);
   
   // 等待執行完成（需要實現等待機制）
   const result = waitForM0JobAndGetSnapshot(jobId, "CSE_SEARCH");
   ```

### 7.2 Human Signal 融入邏輯

**問題**：需要確認 P5 Daily/Weekly 是否有讀取 `HUMAN_SIGNAL` 的邏輯。

**解決方案**：
1. **在 P5 Daily 中讀取 Human Signal**
   ```javascript
   // 在 24_P5_DAILY.js 或相關模組中
   function getUnprocessedHumanSignals() {
     const ss = SpreadsheetApp.getActiveSpreadsheet();
     const sheet = ss.getSheetByName("HUMAN_SIGNAL");
     // 讀取 processed = false 的記錄
     // 返回給 P5 Daily 處理
   }
   ```

2. **在 P5 Weekly 中融入 Human Signal**
   ```javascript
   // 在 24_P5_WEEKLY_DATA.js 中
   function collectHumanSignals() {
     // 讀取最近一週的 Human Signal
     // 作為資訊來源因子融入分析
   }
   ```

### 7.3 緊急通知實現

**Email 通知**（最簡單）
```javascript
function sendEmergencyAlert(alert) {
  const recipient = PropertiesService.getScriptProperties()
    .getProperty("ALERT_EMAIL");
  
  MailApp.sendEmail({
    to: recipient,
    subject: `🚨 緊急警報：${alert.title}`,
    body: alert.message,
    htmlBody: `<h2>${alert.title}</h2><p>${alert.message}</p>`
  });
}
```

**手機通知方案**（進階）
- **Pushover**：簡單的推送服務，支援手機 App
- **Telegram Bot**：免費，易於實現
- **Firebase Cloud Messaging (FCM)**：需要 Google Cloud 設置

---

## 📝 檢查清單總結

### 必須完成（上線前）
- [ ] **P5 Calendar 數據導入** ⭐ **新增**
- [ ] P5 數據收集功能完整（包括財務數據）
- [ ] 五檔股票完整流程測試通過
- [ ] **Phase 結果審查功能實現** ⭐ **新增**
- [ ] UI 功能測試通過（Phase 審查、策略生成、Human Signal、緊急通知）
- [ ] Batch 設定優化完成（能處理 50-100 檔股票）
- [ ] 成本估算完成
- [ ] 正式上線測試通過（50-100 檔股票）

### 建議完成（上線後優化）
- [ ] AI Prompt 精準度進一步優化
- [ ] 數據訂閱規劃（中期）
- [ ] 手機通知實現（進階）

---

## 🎯 下一步行動

1. **立即開始**：補齊 P5 數據收集功能（特別是財務數據收集）
2. **並行進行**：測試五檔股票完整流程
3. **逐步完善**：UI 功能測試、Batch 優化、成本估算
4. **最終驗證**：正式上線測試（50-100 檔股票）

---

**文檔版本**：V1.0  
**最後更新**：2025-01-14  
**狀態**：待執行
