# V8.13 GAS上傳清單 - V7設計實現

**日期**：2026-01-19  
**重要提醒**：所有新增和修改變動的檔案都必須上傳到Google Apps Script (GAS)

---

## 📋 需要上傳到GAS的檔案清單

### ✅ 新增檔案

1. **`src/24_P5_WEEKLY_LEARNING_V7.js`**
   - **狀態**：新增檔案
   - **內容**：V7動態學習系統完整實現
   - **包含功能**：
     - STRATEGY_SNAPSHOT和Claims結構化
     - OUTCOME_SNAPSHOT和四項Scorecard
     - 錯誤歸因引擎
     - LEARNING_STATE管理
     - 情境記憶和Safety Lock
   - **上傳方式**：在GAS編輯器中創建新檔案，複製內容

### ✅ 修改檔案

2. **`src/01_SHEETS_STRUCTURE.js`**
   - **狀態**：修改（新增V7設計的表格Schema）
   - **新增內容**：
     - `P5_STRATEGY_SNAPSHOT_SCHEMA`
     - `P5_OUTCOME_SNAPSHOT_SCHEMA`
     - `P5_LEARNING_STATE_SCHEMA`
     - `P5_SCENARIO_MEMORY_SCHEMA`
   - **上傳方式**：在GAS編輯器中更新現有檔案

3. **`src/24_P5_WEEKLY_CORE.js`**
   - **狀態**：修改（整合V7設計）
   - **修改內容**：
     - Step 5.5：使用`compareStrategyWithRealityEnhanced()`（如果可用）
     - Step 9後：使用`trackStockStrategiesWithSnapshot()`（如果可用）
   - **上傳方式**：在GAS編輯器中更新現有檔案

4. **`src/24_P5_MONTHLY.js`**
   - **狀態**：修改（整合V7設計）
   - **修改內容**：
     - Step 5.5：使用`compareStrategyWithRealityEnhanced()`（如果可用）
   - **上傳方式**：在GAS編輯器中更新現有檔案

5. **`src/24_P5_QUARTERLY.js`**
   - **狀態**：修改（整合V7設計）
   - **修改內容**：
     - Step 3.5：使用`compareStrategyWithRealityEnhanced()`（如果可用）
   - **上傳方式**：在GAS編輯器中更新現有檔案

---

## 📝 上傳步驟

### 步驟1：上傳新增檔案

1. 打開Google Apps Script編輯器
2. 點擊「+」按鈕創建新檔案
3. 命名為：`24_P5_WEEKLY_LEARNING_V7`
4. 複製 `src/24_P5_WEEKLY_LEARNING_V7.js` 的內容
5. 貼上到新檔案中
6. 保存（Ctrl+S 或 Cmd+S）

### 步驟2：更新修改檔案

1. 在GAS編輯器中找到對應的檔案
2. 複製本地修改後的內容
3. 貼上到GAS編輯器中（替換現有內容）
4. 保存（Ctrl+S 或 Cmd+S）

### 步驟3：驗證

1. 檢查所有檔案是否都已上傳
2. 檢查語法錯誤（GAS編輯器會自動檢查）
3. 測試執行（可選）

---

## ⚠️ 重要提醒

**永久記憶**：以後所有新增和修改變動的檔案都必須上傳到Google Apps Script (GAS)

**檢查清單**：
- [ ] 新增檔案已上傳
- [ ] 修改檔案已更新
- [ ] 語法檢查通過
- [ ] 功能測試通過（可選）

---

**此文檔用於追蹤需要上傳到GAS的檔案，確保所有變更都同步到GAS環境。**
